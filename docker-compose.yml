services:
  englishpod:
    build: .
    image: englishpod
    container_name: englishpod
    ports:
      - "${PORT:-4173}:4173"
    volumes:
      # 课程资源 (音频/字幕/PDF/词典) 挂载进容器, 只读
      - ./resource:/app/resource:ro
    environment:
      - PORT=4173
    restart: unless-stopped
